apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base
- configmap.yaml

patches:
- patch: |-
    - op: replace
      path: /spec/ports/0/nodePort
      value: 30081
  target:
    kind: Service
    name: kargo-demo
images:
- name: artifactory.sandbox.gitops.1keyes.net/te-docker-release/gitops/test-image
  newTag: 1.1.0
